# JDCRS — Lite Version

Use ONLY this environment:

docker exec jarvis-app bash -c "poetry run <cmd>"

Never use host python, host poetry, or system venv.
Run everything inside container + poetry.
If unsure: STOP and ask.
